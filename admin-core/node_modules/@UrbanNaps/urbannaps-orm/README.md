# Awesome Project Build with TypeORM

Steps to run this project:

1. Run `npm i` command
2. Setup database settings inside `data-source.ts` file
3. Run `npm start` command
4. update `$NPM_TOKEN` in `.npmrc` to pull private npm packages

to Publish:
`npm version patch && export NPM_TOKEN="<<token>>" && npm publish`
